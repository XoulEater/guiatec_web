---
import Layout from "../../layouts/MainLayout.astro";
import type { WorkPlanDTO } from "../../lib/data";
import WorkPlan from "../../components/WorkPlan.tsx";
import { workPlans } from "../../lib/data";

const { id } = Astro.params;

const workPlan = workPlans.find((workPlan: WorkPlanDTO) => workPlan.id === id);
---

<Layout title="GuíaTEC" currentRoute="equipo">
  <WorkPlan workplanDTO={workPlan} client:only />
</Layout>
